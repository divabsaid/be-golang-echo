# be-golang-echo

```
migrate -path db/migrations -database "mysql://root@tcp(localhost:3306)/todo" -verbose up

migrate -path db/migrations -database "mysql://root@tcp(localhost:3306)/todo" -verbose down
```